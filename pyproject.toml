[project]
name = "home-interior-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fal-client>=0.7.0",
    "flask>=3.1.1",
]
